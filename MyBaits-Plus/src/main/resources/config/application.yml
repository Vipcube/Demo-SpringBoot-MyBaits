server:
  port: 8080

spring:
  application:
    name: demo-spring-mybatis-plus

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/test # standard alone
    url: jdbc:mysql://localhost:3307/sharding_test # proxy
    username: root
    password: test123456

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml

logging:
  level:
    root: warn
    org.vipcube.spring.boot.mybatis.mapper: trace
